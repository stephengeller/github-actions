function foo() {
    console.log("bar")
}

/**
 * Returns the plural form of a given word based on the provided count.
 *
 * @param num - The word to pluralize.
 * @returns The word in its singular form if `count` is 1, otherwise the plural form.
 */
function bar(num: number) {
    console.log(num)
}

