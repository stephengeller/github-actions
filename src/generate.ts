// Placeholder — implemented in Phase 4.
import type { Violation } from "./types";

export async function generateTsDoc(_args: {
  apiKey: string;
  violation: Violation;
}): Promise<string> {
  throw new Error("generateTsDoc not implemented (Phase 4)");
}
